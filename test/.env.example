# Copy this file to test/.env and fill in real values.
# test/.env is gitignored (everything under test/ is), so secrets never get committed.

# Needed by ai_weather_briefing.py (Gemini call)
GOOGLE_API_KEY=
